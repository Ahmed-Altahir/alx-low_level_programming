#include "main.h"

/**
 * Description: mul - check if c is upper
 *
 * @a: input for alphabet
 * @b: input for alphabet
 *
 * Return: 1 if its uppper, 0 if not
*/

int mul(int a, int b)
{
	return (a * b);
}
